export interface SpriteThumbnailsOptions {\n  url: string;      // e.g. `sprite-{index}.webp`\n  width: number;    // single thumbnail tile width\n  height: number;   // single thumbnail tile height\n  columns: number;  // number of columns in each sprite sheet\n  rows: number;     // number of rows in each sprite sheet\n  interval: number; // seconds between captured frames\n}\n\n// Augment video.js Player with the plugin method if @types/video.js are present.\ndeclare module 'video.js' {\n  interface VideoJsPlayer {\n    spriteThumbnails?: (options?: SpriteThumbnailsOptions) => void;\n  }\n}\n